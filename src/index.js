import React from "react";
import ReactDOM from "react-dom/client";
import Patalmayadevam from "./Patalmayadevam";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <Patalmayadevam />
  </React.StrictMode>
);
